# Per-user instance config generated by a recipe's bootstrap.
# The tracked template is state.template.json; a user's real state.json stays local.
**/state.json

# Per-user client roster generated by a recipe's bootstrap. Holds real client names, asset
# paths, and approver addresses. The tracked template is client-roster.template.csv.
**/client-roster.csv
**/client-roster.xlsx

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
